require 'spec_helper'

describe Fastafarian do
  it 'has a version number' do
    expect(Fastafarian::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
